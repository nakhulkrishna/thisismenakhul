export const PERSONAL = {
  name: "Nakhul Krishna",
  role: "Software Engineer",
  tagline: "Building production-grade mobile apps with Flutter",
  bio: "Software Engineer experienced in building and shipping production-grade Android and iOS applications for startups, healthcare platforms, and client projects. Skilled in Clean Architecture, Riverpod, Firebase, REST APIs, and scalable mobile architecture — from design to deployment.",
  location: "India",
  email: "nakhulkrishna253@gmail.com",
  github: "https://github.com/nakhulkrishna",
  linkedin: "https://linkedin.com/in/nakhulkrishna",
  twitter: "https://twitter.com/nakhulkrishna",
};

export const SKILLS = [
  { category: "Languages", items: ["Dart", "Swift"] },
  { category: "Frameworks & Platforms", items: ["Flutter", "Firebase", "Android", "iOS"] },
  { category: "Architecture", items: ["Clean Architecture", "Repository Pattern", "SOLID Principles", "Modular Architecture"] },
  { category: "State Management", items: ["Riverpod", "BLoC", "Provider"] },
  { category: "Backend & Databases", items: ["REST APIs", "Firebase Auth", "Cloud Firestore", "FCM", "SQLite", "Hive"] },
  { category: "Mobile Dev", items: ["Push Notifications", "Deep Linking", "Performance Optimization", "Responsive UI"] },
  { category: "Tools & DevOps", items: ["Git", "GitHub", "Postman", "Android Studio", "Xcode", "VS Code"] },
  { category: "Deployment", items: ["Google Play Store", "TestFlight", "APK/AAB Builds", "App Release Management"] },
];

export const PROJECTS = [
  {
    id: 1,
    title: "Docwo Healthcare Platform",
    tagline: "Healthcare mobile platform — Founding Engineer",
    description:
      "Architected and developed a healthcare mobile platform from the ground up as a founding engineering contributor. Designed end-to-end mobile architecture and led all technical decision-making.",
    tech: ["Flutter", "Dart", "Clean Architecture", "Riverpod", "Firebase", "REST APIs"],
    highlights: [
      "Architected scalable mobile structure using Clean Architecture and repository patterns",
      "Built core healthcare workflows: authentication, appointment management, and real-time data",
      "Led technical decisions for project structure, state management, and API integration",
      "Developed reusable modules and coding standards to accelerate future development",
    ],
    color: "#00b4d8",
    badge: "Startup",
    github: "",
    live: "https://play.google.com/store/apps/details?id=com.docwo.app",
    liveLabel: "Play Store",
  },
  {
    id: 2,
    title: "Feed Meals",
    tagline: "Food delivery — App Store",
    description:
      "Production-grade food delivery application built for iOS with real-time order management, seamless payment integrations, and optimized performance.",
    tech: ["Flutter", "Dart", "Firebase", "REST APIs", "Clean Architecture", "Riverpod"],
    highlights: [
      "Real-time order tracking and management workflows",
      "Payment gateway integration and third-party SDK integrations",
      "Push notifications and deep linking",
      "Optimized UI rendering and state management for smooth performance",
    ],
    color: "#06d6a0",
    badge: "Production",
    github: "",
    live: "https://apps.apple.com/in/app/feed-meals/id6738683868",
    liveLabel: "App Store",
  },
  {
    id: 3,
    title: "Finex",
    tagline: "Finance app — App Store",
    description:
      "Production-grade iOS finance application built with SwiftUI, shipped on the App Store with a polished native user experience and clean architecture.",
    tech: ["SwiftUI", "Swift", "iOS", "Xcode"],
    highlights: [
      "Shipped to App Store with full release management",
      "Scalable architecture using Clean Architecture and repository patterns",
      "Integrated third-party APIs and payment workflows",
    ],
    color: "#f59e0b",
    badge: "Production",
    github: "",
    live: "https://apps.apple.com/in/app/finexs/id6761826860",
    liveLabel: "App Store",
  },
  {
    id: 4,
    title: "Red Rose Dates",
    tagline: "Real-time sales management — Qatar",
    description:
      "Built and shipped a real-time sales management web app for an international client in Qatar, handling architecture, development, testing, and deployment independently.",
    tech: ["Flutter", "Dart", "Firebase", "Cloud Firestore", "Clean Architecture"],
    highlights: [
      "Admin and sales workflows with Firestore-based real-time synchronization",
      "Improved operational tracking efficiency by 40%",
      "Order and billing workflows reduced manual processing errors by 35%",
      "Managed complete SDLC from requirements to deployment",
    ],
    color: "#e63946",
    badge: "Freelance",
    github: "",
    live: "https://products-managment-a23a7.web.app",
    liveLabel: "Live App",
  },
  {
    id: 5,
    title: "Planet Zero Customer App",
    tagline: "Food delivery platform",
    description:
      "Production-grade food delivery application built for Android and iOS with real-time order management, seamless payment integrations, and optimized performance.",
    tech: ["Flutter", "Dart", "Firebase", "REST APIs", "Clean Architecture", "Riverpod"],
    highlights: [
      "Real-time order tracking and management workflows",
      "Payment gateway integration and third-party SDK integrations",
      "Push notifications and deep linking",
      "Optimized UI rendering and state management for smooth performance",
    ],
    color: "#10b981",
    badge: "Production",
    github: "",
    live: "",
    liveLabel: "",
  },
  {
    id: 6,
    title: "Bootcamp Management Platform",
    tagline: "Student & operations management — India",
    description:
      "Designed and developed a Flutter-based management platform for a bootcamp organization, handling architecture, development, testing, and deployment independently.",
    tech: ["Flutter", "Dart", "Firebase", "Cloud Firestore", "Clean Architecture"],
    highlights: [
      "Real-time tracking for student activities, playtime, and attendance",
      "Financial management modules for payments, expenses, and business operations",
      "Role-based workflows for administrators and staff",
      "Centralized dashboard with real-time sync and reporting",
    ],
    color: "#7b2d8b",
    badge: "Freelance",
    github: "",
    live: "",
    liveLabel: "",
  },
];

export const EXPERIENCE = [
  {
    role: "Software Engineer",
    company: "Code-ox Technologies LLP",
    period: "2025 – Present",
    type: "Full-time",
    highlights: [
      "Delivered production-grade Flutter apps across healthcare, logistics, and business management domains",
      "Implemented scalable solutions using Clean Architecture, Riverpod, and repository patterns",
      "Integrated REST APIs, Firebase, payment gateways, push notifications, and third-party SDKs",
      "Participated in code reviews, debugging, testing, and production deployments",
    ],
  },
  {
    role: "Mobile Architect",
    company: "Docwo Healthcare Platform",
    period: "2025 – Present",
    type: "Equity-Based Startup",
    highlights: [
      "Architecting a healthcare mobile platform from the ground up as a founding engineering contributor",
      "Designed scalable architecture using Clean Architecture, Riverpod, and repository patterns",
      "Led technical decision-making for structure, state management, and API integration",
      "Built core workflows: authentication, appointment management, and real-time data interactions",
    ],
  },
  {
    role: "Junior Flutter Developer",
    company: "Spinecode Solutions",
    period: "Jun 2024 – Oct 2024",
    type: "Full-time",
    highlights: [
      "Contributed to 6+ client apps across e-learning, logistics, and booking domains",
      "Integrated payment gateways, notifications, and third-party APIs",
      "Improved user engagement by 25% across key workflows",
      "Reduced bug-fix turnaround time by 30% through Agile sprint support",
    ],
  },
];

export type InsightSection = { heading: string; body: string; code?: string };

export type Insight = {
  slug: string;
  title: string;
  summary: string;
  tag: string;
  readTime: string;
  sections: InsightSection[];
};

export const INSIGHTS: Insight[] = [
  {
    slug: "clean-architecture-flutter",
    title: "Clean Architecture in Flutter: A Practical Guide",
    summary: "Forget the theory. Here's the exact folder structure and dependency injection pattern I use in every new Flutter project to keep it maintainable at scale.",
    tag: "Architecture",
    readTime: "8 min",
    sections: [
      {
        heading: "Why architecture matters early",
        body: "When you start a Flutter project with just a few screens, any structure works. The pain comes at screen 15 when your state logic is scattered, your API calls live inside widgets, and onboarding a new dev takes a full day. Clean Architecture solves this by making the rules explicit: UI never knows about data sources, business logic never imports Flutter widgets.",
      },
      {
        heading: "The three layers",
        body: "I split every feature into three layers — Presentation, Domain, and Data. The Domain layer is pure Dart: entities and use cases with zero Flutter or Firebase imports. The Data layer implements repositories and talks to APIs or Firestore. The Presentation layer is Flutter widgets and state management (Riverpod). Dependencies always point inward — Data depends on Domain, Presentation depends on Domain, nothing depends on Presentation.",
      },
      {
        heading: "Folder structure I use",
        body: "Each feature gets its own folder under lib/features/. Inside each feature: data/ (models, repos, sources), domain/ (entities, repo interfaces, use cases), and presentation/ (pages, widgets, providers). Shared code lives in lib/core/.",
        code: `lib/
├── core/
│   ├── errors/
│   ├── network/
│   └── di/           ← dependency injection setup
├── features/
│   └── auth/
│       ├── data/
│       │   ├── models/
│       │   ├── repositories/
│       │   └── sources/
│       ├── domain/
│       │   ├── entities/
│       │   ├── repositories/  ← abstract interfaces
│       │   └── usecases/
│       └── presentation/
│           ├── pages/
│           ├── widgets/
│           └── providers/`,
      },
      {
        heading: "Dependency injection with get_it",
        body: "I register everything in a single setup file using get_it. Data sources → repositories → use cases → providers, all wired in order. This means swapping a real API for a mock in tests is a one-line change, and nothing in your widget tree ever calls a constructor directly.",
        code: `// core/di/injection.dart
final sl = GetIt.instance;

Future<void> init() async {
  // Use cases
  sl.registerLazySingleton(() => LoginUseCase(sl()));

  // Repositories
  sl.registerLazySingleton<AuthRepository>(
    () => AuthRepositoryImpl(remoteSource: sl()),
  );

  // Data sources
  sl.registerLazySingleton<AuthRemoteSource>(
    () => AuthRemoteSourceImpl(client: sl()),
  );
}`,
      },
      {
        heading: "The rule I never break",
        body: "Domain layer imports nothing except Dart core and other domain classes. The moment you import flutter/material.dart or firebase_core inside a use case, the architecture is broken. I enforce this with a lint rule. Keep domain pure — it's the heart of the app and should be testable in isolation with zero setup.",
      },
    ],
  },
  {
    slug: "ads-sdk-flutter",
    title: "Integrating Google & Meta Ads SDKs in Flutter",
    summary: "A practical walkthrough of setting up Google AdMob and Meta Audience Network in Flutter — handling ad load failures, mediation, and avoiding the pitfalls that tank your eCPM.",
    tag: "Monetization",
    readTime: "6 min",
    sections: [
      {
        heading: "Setting up AdMob",
        body: "Add google_mobile_ads to pubspec.yaml and initialise it once at app startup before any ad loads. Put your App ID in AndroidManifest.xml and Info.plist. One mistake I see constantly: calling MobileAds.instance.initialize() inside a widget's build method. Do it in main() and await it before runApp — this alone fixes most 'ad not loading' bugs.",
        code: `Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  await MobileAds.instance.initialize();
  runApp(const MyApp());
}`,
      },
      {
        heading: "Preloading ads the right way",
        body: "Never load an ad and show it in the same frame. Load banner and interstitial ads ahead of time — on screen init or after a key user action — and cache the loaded ad instance. For interstitials, I keep a simple AdManager singleton that preloads the next ad as soon as the current one is dismissed.",
        code: `class InterstitialAdManager {
  InterstitialAd? _ad;

  void load() {
    InterstitialAd.load(
      adUnitId: AdConfig.interstitialId,
      request: const AdRequest(),
      adLoadCallback: InterstitialAdLoadCallback(
        onAdLoaded: (ad) {
          _ad = ad;
          _ad!.fullScreenContentCallback = FullScreenContentCallback(
            onAdDismissedFullScreenContent: (_) => load(), // preload next
          );
        },
        onAdFailedToLoad: (error) => Future.delayed(
          const Duration(seconds: 30), load, // retry after backoff
        ),
      ),
    );
  }

  void show() => _ad?.show();
}`,
      },
      {
        heading: "Meta Audience Network mediation",
        body: "Adding Meta as a mediation source through AdMob can double eCPM in certain regions. Install the Meta mediation adapter package, add your Meta App ID to both platform manifests, and configure the mediation waterfall in the AdMob dashboard. The SDK itself needs ATT permission on iOS — always request it before initialising ads on iOS 14+.",
      },
      {
        heading: "Pitfalls that tank eCPM",
        body: "Three things I've seen destroy ad revenue in production: showing ads too frequently (ad fatigue drops fill rate), using test ad unit IDs in production (they return zero revenue), and not handling ad load failures gracefully so the slot stays blank. Always implement a retry with exponential backoff on load failure, and track fill rate as a metric alongside revenue.",
      },
    ],
  },
  {
    slug: "conversion-tracking-flutter",
    title: "App Conversion Tracking: Firebase vs Adjust vs AppsFlyer",
    summary: "How I set up conversion tracking in production Flutter apps — install attribution, in-app event tracking, and wiring it up to Google Ads and Meta campaigns.",
    tag: "Analytics",
    readTime: "7 min",
    sections: [
      {
        heading: "Why conversion tracking matters",
        body: "Without attribution, you can't tell which campaign drove an install, which ad creative leads to purchases, or whether your Google Ads spend is profitable. Every production app I ship now has at least Firebase Analytics wired up from day one — retrofitting it later is painful.",
      },
      {
        heading: "Firebase Analytics — the baseline",
        body: "Firebase Analytics is free and gives you install counts, session data, and custom events out of the box. Log a purchase event with a value parameter and Google Ads will automatically import it as a conversion once you link your Firebase project. This is the minimum setup for any app.",
        code: `await FirebaseAnalytics.instance.logEvent(
  name: 'purchase',
  parameters: {
    'currency': 'USD',
    'value': 9.99,
    'transaction_id': orderId,
  },
);`,
      },
      {
        heading: "When to add Adjust or AppsFlyer",
        body: "Firebase attribution breaks down with iOS SKAdNetwork — it can't give you user-level install data post ATT. If you're running paid UA campaigns on Meta or TikTok and need accurate ROAS measurement, add Adjust or AppsFlyer. Both have Flutter SDKs. AppsFlyer has better out-of-box Meta integration; Adjust has a cleaner Flutter SDK in my experience.",
        code: `// AppsFlyer init
AppsflyerSdk appsflyerSdk = AppsflyerSdk({
  "afDevKey": "YOUR_DEV_KEY",
  "afAppId": "YOUR_APP_ID",
  "isDebug": false,
});
await appsflyerSdk.initSdk(
  registerConversionDataCallback: true,
  registerOnAppOpenAttributionCallback: true,
);

// Log a conversion event
appsflyerSdk.logEvent("af_purchase", {
  "af_revenue": 9.99,
  "af_currency": "USD",
});`,
      },
      {
        heading: "Wiring up to ad campaigns",
        body: "For Google Ads: link Firebase in the Google Ads console and import Firebase events as conversions. For Meta: use the Meta SDK or Conversions API via your backend for better iOS signal. Always pass a deduplicated event ID when using both client-side SDK events and server-side Conversions API — otherwise Meta double-counts.",
      },
      {
        heading: "What I track in every app",
        body: "At minimum: first_open, sign_up (with method), purchase (with value + currency), and a key engagement event specific to the app (e.g. order_placed, appointment_booked). These four events give you enough signal to optimise campaigns and calculate LTV from day one.",
      },
    ],
  },
  {
    slug: "flutter-performance",
    title: "Flutter Performance: 60fps is the Baseline",
    summary: "The techniques I use to hit smooth performance on real devices — const widgets, build method discipline, ListView.builder, and isolating heavy work off the UI thread.",
    tag: "Performance",
    readTime: "5 min",
    sections: [
      {
        heading: "const is free — use it everywhere",
        body: "Every widget marked const is created once and never rebuilt. Audit your widget tree and add const wherever the widget has no dynamic data. This single habit, applied consistently, is the highest-ROI performance improvement in Flutter. Enable the prefer_const_constructors lint rule so the analyser catches missed opportunities automatically.",
      },
      {
        heading: "Build method discipline",
        body: "The build method should only read state and return a widget tree. No expensive computations, no string formatting loops, no sorting lists inside build. Move that logic into a notifier or compute it once in initState. If a build method takes longer than ~1ms, it will cause jank on slower devices.",
      },
      {
        heading: "ListView.builder over ListView",
        body: "ListView renders all its children upfront. ListView.builder renders only the items currently visible on screen. For any list longer than 10–15 items, always use .builder. Same principle applies to GridView, PageView, and CustomScrollView with SliverList.",
        code: `// Bad — renders all 1000 items
ListView(children: items.map((e) => ItemWidget(e)).toList())

// Good — renders only visible items
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) => ItemWidget(items[index]),
)`,
      },
      {
        heading: "Heavy work off the UI thread",
        body: "JSON parsing, image processing, encryption, and database queries should never run on the main isolate. Use compute() for one-off heavy tasks or spawn a dedicated Isolate for ongoing work. I've fixed more than a few 'the app freezes on load' bugs by moving a large JSON decode into compute().",
        code: `// Move heavy parsing off the UI thread
final result = await compute(parseHeavyJson, rawJsonString);`,
      },
      {
        heading: "Profile on a real device",
        body: "The Flutter DevTools performance tab on a simulator is misleading — simulators don't have GPU constraints. Always profile on the lowest-spec real device in your target market in profile mode (flutter run --profile). Look for janky frames in the frame chart and drill into the flame graph to find the culprit widget.",
      },
    ],
  },
  {
    slug: "fcm-deep-linking",
    title: "Push Notifications Done Right: FCM + Deep Linking",
    summary: "How I wire up Firebase Cloud Messaging with deep linking so every notification lands the user exactly where they need to be — on both cold start and foreground.",
    tag: "Mobile Dev",
    readTime: "5 min",
    sections: [
      {
        heading: "The three notification states",
        body: "FCM behaves differently depending on app state: foreground (app open), background (app suspended), and terminated (app closed). You need separate handling for all three. Most notification bugs I've debugged come from only testing foreground and missing the terminated-state cold start flow.",
      },
      {
        heading: "Handling all three states",
        body: "In main(), check getInitialMessage() for the terminated state. Use onMessageOpenedApp stream for background taps. Use onMessage stream for foreground messages — here you typically show a local notification since FCM doesn't display a system notification when the app is open.",
        code: `// main.dart — terminated state
final initialMessage = await FirebaseMessaging.instance.getInitialMessage();
if (initialMessage != null) _handleMessage(initialMessage);

// background tap
FirebaseMessaging.onMessageOpenedApp.listen(_handleMessage);

// foreground — show local notification manually
FirebaseMessaging.onMessage.listen((message) {
  LocalNotificationService.show(message);
});`,
      },
      {
        heading: "Deep linking from notifications",
        body: "Add a route or screen key to every notification payload. In _handleMessage(), parse that key and push the correct route. I use go_router — the notification handler calls context.push() with the route from the payload. This keeps navigation logic centralised rather than scattered across notification handlers.",
        code: `void _handleMessage(RemoteMessage message) {
  final route = message.data['route'];
  if (route != null) {
    navigatorKey.currentContext?.push(route);
  }
}`,
      },
      {
        heading: "iOS permissions and token management",
        body: "On iOS you must explicitly request permission before FCM will deliver notifications. Request it early but not on first launch — a permission prompt at the wrong moment gets denied. I trigger it after a meaningful action (e.g. after first order placed). Also refresh and upload the FCM token on every app open — tokens rotate and stale tokens cause silent delivery failures.",
      },
    ],
  },
  {
    slug: "payment-gateway-flutter",
    title: "Payment Gateway Integration in Flutter",
    summary: "My battle-tested approach to integrating Stripe, Razorpay, and regional gateways — handling webhooks, retry logic, and keeping the checkout flow from breaking on edge cases.",
    tag: "Integrations",
    readTime: "6 min",
    sections: [
      {
        heading: "Never trust the client",
        body: "The cardinal rule: payment intent creation and order confirmation must happen server-side. The Flutter app collects card details and initiates the flow, but the amount, currency, and order ID are set by your backend. If your app sends the amount to the server and the server trusts it, users can modify the request and pay $0.01 for anything.",
      },
      {
        heading: "Stripe integration pattern",
        body: "The flow I use: Flutter calls your backend → backend creates a PaymentIntent and returns the client secret → Flutter passes the client secret to the Stripe Flutter SDK to present the payment sheet → on success, your backend webhook confirms the payment server-side before fulfilling the order.",
        code: `// 1. Get client secret from your backend
final response = await api.createPaymentIntent(amount: total);

// 2. Init Stripe payment sheet
await Stripe.instance.initPaymentSheet(
  paymentSheetParameters: SetupPaymentSheetParameters(
    paymentIntentClientSecret: response.clientSecret,
    merchantDisplayName: 'Your App',
  ),
);

// 3. Present sheet
await Stripe.instance.presentPaymentSheet();

// 4. Wait for webhook confirmation before showing success`,
      },
      {
        heading: "Handling failures gracefully",
        body: "Payment failures are normal — card declines, network timeouts, 3DS challenges that time out. Always catch StripeException and map error codes to user-friendly messages. Never show raw gateway error strings to users. Have a retry flow that preserves the cart state so users don't lose their order after a decline.",
      },
      {
        heading: "Razorpay and regional gateways",
        body: "For India and Middle East markets I've used Razorpay and regional gateways. The integration pattern is the same — server creates order, client gets order ID, SDK presents payment UI, webhook confirms. The main difference is Razorpay uses an order ID rather than a client secret. Always test with actual test cards on a real device — emulator behaviour differs for 3DS redirects.",
      },
      {
        heading: "Idempotency keys",
        body: "If the network drops after the user pays but before your app gets the response, you risk charging them twice on retry. Always pass an idempotency key (a unique ID per payment attempt, usually the order ID) to your backend and have the backend pass it to the gateway. This guarantees retrying the same request returns the same result without a double charge.",
      },
    ],
  },
  {
    slug: "riverpod-vs-bloc",
    title: "Riverpod Over BLoC: When and Why",
    summary: "After shipping apps with both, here's when I reach for Riverpod and when BLoC still wins — a practical comparison grounded in real production decisions.",
    tag: "State Management",
    readTime: "4 min",
    sections: [
      {
        heading: "What I actually use day-to-day",
        body: "I default to Riverpod for new projects. It has less boilerplate, compile-time safety, and the provider/notifier model maps naturally to how I think about features. BLoC is still my pick when I'm working on a team where junior devs need explicit guidance — the event → state contract makes code reviews easier.",
      },
      {
        heading: "Riverpod: what I like",
        body: "AsyncNotifier handles loading/error/data states with almost no boilerplate. Providers are globally accessible without BuildContext. ref.watch() in widgets is clean. The code generation with riverpod_generator removes the last remaining friction. For features like auth state, user profile, or cart — Riverpod is faster to write and easier to read.",
        code: `@riverpod
class CartNotifier extends _$CartNotifier {
  @override
  List<CartItem> build() => [];

  void add(CartItem item) => state = [...state, item];
  void remove(String id) => state = state.where((e) => e.id != id).toList();
}`,
      },
      {
        heading: "BLoC: when it still wins",
        body: "BLoC shines when state transitions are complex and need to be auditable — think multi-step checkout flows, real-time order status, or anything where you want a clear log of every event and state change. The explicit Event → State contract also helps when onboarding developers onto a large existing codebase, since it's self-documenting.",
      },
      {
        heading: "The one thing neither handles well",
        body: "Both Riverpod and BLoC require discipline around disposal and scope. A provider or BLoC that outlives its screen will hold onto resources and cause memory leaks. I always scope state to the smallest widget subtree that needs it, and explicitly override/dispose when navigating away. This is where most state management bugs actually come from — not the library choice.",
      },
    ],
  },
  {
    slug: "app-store-release-management",
    title: "App Store & Play Store Release Management",
    summary: "The release checklist I follow for every production app — from TestFlight builds and phased rollouts to handling rejection reviews and managing AAB/APK signing.",
    tag: "Deployment",
    readTime: "5 min",
    sections: [
      {
        heading: "Build variants and flavors",
        body: "Every app I ship has at least two flavors: dev and production. Dev points to staging APIs, uses debug Firebase projects, and has debug banners enabled. Production is the build that goes to the stores. Flutter flavors with --dart-define or flavor-specific main files keep these completely separate — no risk of accidentally shipping a staging API URL.",
        code: `# Dev build
flutter run --flavor dev --target lib/main_dev.dart

# Production build
flutter build appbundle --flavor prod --target lib/main_prod.dart`,
      },
      {
        heading: "iOS: TestFlight workflow",
        body: "I distribute internal builds to TestFlight before every release. Archive in Xcode with the Production scheme, upload to App Store Connect, and share the TestFlight link with stakeholders for sign-off. Run TestFlight for at least 2–3 days before submitting for App Store review — this catches issues that only appear with real-world data and devices.",
      },
      {
        heading: "Android: AAB and signing",
        body: "Always submit an AAB (Android App Bundle) to Play Store rather than an APK — it enables smaller download sizes via asset delivery. Keep your keystore file and key passwords in a secure location outside the repo. I store signing config in a local.properties file that's gitignored, and document the process in the team wiki so a lost keystore doesn't kill the app.",
        code: `# android/key.properties (gitignored)
storeFile=../keystore/release.jks
storePassword=yourStorePassword
keyAlias=yourKeyAlias
keyPassword=yourKeyPassword`,
      },
      {
        heading: "Phased rollout and monitoring",
        body: "On Google Play I always use a phased rollout — start at 10%, watch crash-free rate and ANR rate in Play Console for 24 hours, then expand. On App Store, phased release rolls out over 7 days automatically. Have Firebase Crashlytics open on release day. If crash-free rate drops below 99%, halt the rollout immediately.",
      },
      {
        heading: "Handling App Store rejection",
        body: "Most rejections I've seen fall into three categories: missing privacy policy URL, inadequate demo account in the review notes, or guideline 4.2 (minimal functionality). For 4.2 rejections on B2B apps, I add a demo mode that shows sample data so reviewers can evaluate the app without a real account. Always respond to rejections within 24 hours — faster responses mean faster re-review.",
      },
    ],
  },
];
